# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
env/
venv/

# Scraper output files (keep main CSV only)
cr-tracker/beacon_exclusives.csv
cr-tracker/scraper_output.log
cr-tracker/*.html

# macOS
.DS_Store

# IDE
.vscode/
.idea/

# Genshin — HoYoLAB cookies (never commit!)
genshin/config.json

# Audition analytics — credentials and session tokens (never commit!)
audition-analytics/config.json
audition-analytics/.v123_session.json
audition-analytics/.v123_localstorage.json
audition-analytics/auditions.db
audition-analytics/debug_*.json
audition-analytics/debug_*.html

# Beacon scheduler secrets (never commit these!)
beacon-scheduler/token.json
beacon-scheduler/credentials.json

# Node
node_modules/
package-lock.json

# Cloudflare Worker build artifacts
genshin/worker/.wrangler/
